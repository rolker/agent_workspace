"""ros_manifest — prototype general ROS 2 multi-repo manifest resolver.

**Extraction candidate.** See agent_workspace issue #267: this package (name, CLI command, and
directory) is a placeholder and is designed to be extracted into its own repository. Nothing
outside ``tools/ros-manifest/`` imports this package directly; the intended integration surface
is the ``ros-manifest`` CLI only.
"""

__version__ = "0.1.0"
